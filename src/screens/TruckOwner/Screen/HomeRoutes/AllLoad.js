import {
  View,
  Text,
  Image,
  StyleSheet,
  Dimensions,
  ScrollView,
} from 'react-native';
import React from 'react';
import Card from './Utils/Card';

const AllLoad = () => {
  return (
    <ScrollView>
      <Card />
    </ScrollView>
  );
};

export default AllLoad;
